import { runPipeline } from "../core/pipeline";

console.log("Watch mode placeholder - rerun pipeline on changes (not implemented)");

// Minimal placeholder: run once
runPipeline().catch(err => console.error(err));
