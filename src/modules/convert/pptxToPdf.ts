import path from "path";

export async function pptxToPdf(pptxPath: string, outPdf: string) {
  // Stub: in a real implementation call libreoffice/soffice or cloud API
  console.log(`pptxToPdf: ${pptxPath} -> ${outPdf}`);
  return outPdf;
}
