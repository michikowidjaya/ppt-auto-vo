export async function renderScene(imagePath: string, audioPath: string, outPath: string) {
  // Stub: create a short video from image+audio (ffmpeg)
  console.log(`renderScene ${imagePath} + ${audioPath} -> ${outPath}`);
  return outPath;
}
