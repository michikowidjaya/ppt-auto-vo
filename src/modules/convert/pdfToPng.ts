export async function pdfToPng(pdfPath: string, outDir: string) {
  // Stub: use pdftoppm or imagemagick in real implementation
  console.log(`pdfToPng: ${pdfPath} -> ${outDir}`);
  return ["slide-01.png"];
}
